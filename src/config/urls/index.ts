const BASE_URL = '/';

export const urls = {
  root: BASE_URL,
  composition: `/composition`,
  colors: `/colors`,
  animals: `/animals`,
  fonts: `/fonts`,
  success: `/success`,
};

export default urls;
